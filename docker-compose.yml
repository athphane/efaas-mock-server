services:
  efaas:
    build: .
    container_name: efaas
    hostname: efaas
    network_mode: host
    environment:
      - HOST=0.0.0.0
      - PORT=5000
      - SERVER_URL=http://localhost:5000
      - SEED_COUNT=100
